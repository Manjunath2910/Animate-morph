import bg from "@/imports/section4/bg.png";

// ─── Section 4 — "Sending money home should feel as easy as sending a message" ──
// Full-width blue background + comparison table (exact Figma image).
export default function Section4() {
  return (
    <section style={{ width: "100%", backgroundColor: "#FFFBF2", overflow: "hidden" }}>
      <img
        src={bg}
        alt="Sending money home should feel as easy as sending a message"
        style={{ width: "100%", height: "auto", display: "block" }}
      />
    </section>
  );
}
